# Screenshots

Drop screenshots here for the main README to display.

Expected files:

| File | What it should show | Suggested size |
|---|---|---|
| `desktop.png` | The main ShellPort UI in a desktop browser — terminal with a few session tabs visible | ~1400×900, PNG |
| `mobile.png` | ShellPort on a phone — terminal + the bottom mobile toolbar (Enter/Tab/arrows/Ctrl) | ~390×844 (iPhone-ish), PNG |

Tips:
- Use a clean terminal (no secrets in scrollback)
- Crop browser chrome if you want a cleaner look
- Keep file size reasonable (<500 KB each) so the README loads fast on GitHub
